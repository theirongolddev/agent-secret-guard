#!/bin/bash
# Compatibility adapter: Claude PostToolUse redaction via Agent Secret Guard.
set -euo pipefail
. "/Users/tayloreernisse/.local/bin/asg-hook-lib"
if OUTPUT=$("/Users/tayloreernisse/.local/bin/asg-fast" claude-post "$@" 2>/dev/null); then
  printf '%s\n' "$OUTPUT"
  exit 0
fi
asg_claude_posttool_fail_closed claude
