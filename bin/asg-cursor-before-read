#!/bin/bash
set -euo pipefail
. "{{ASG_HOME}}/.local/bin/asg-hook-lib"
if OUTPUT=$({{ASG_HOME}}/.local/bin/asg-fast cursor-hook --event beforeReadFile "$@" 2>/dev/null); then
  printf '%s\n' "$OUTPUT"
  exit 0
fi
asg_cursor_fail_closed beforeReadFile
